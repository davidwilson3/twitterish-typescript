import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#1da1f2"
    },
    secondary: {
      dark: "#657786",
      main: "#AAB8C2",
      light: "#e1e8ed"
    }
  }
});

export default theme;
